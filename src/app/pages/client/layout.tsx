import React from "react";

// Local layout for client page: no sidebar, plain container
const ClientLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default ClientLayout;
